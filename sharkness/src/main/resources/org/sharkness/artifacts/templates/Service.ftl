package ${basePackage}.service;

import ${completePathOfClassId};
import ${basePackage}.entity.${model};
import org.sharkness.business.support.ModelService;

public interface ${model}Service extends ModelService<${classId}, ${model}> {

}